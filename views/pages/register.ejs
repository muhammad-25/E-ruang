<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title> <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="/css/register.css">
</head>
<body>

    <div class="register-container">
        
        <div class="register-image-section"></div>

        <div class="register-form-section">
            <h2 class="register-title">REGISTER</h2>
            <% if (errors && errors.length) { %>
                <ul>
                <% errors.forEach(function(err){ %>
                    <li><%= err.msg %></li>
                <% }) %>
                </ul>
            <% } %>

            <form action="/register" method="POST">
                
                <div class="input-group">
                    <label for="email">Email</label>
                    <div class="input-field-container">
                        <input type="email" id="email" name="email" placeholder="Email" required value="<%= (old && old.email) ? old.email :'' %>">
                    </div>
                </div>

                <div class="input-group">
                    <label for="name">nama</label>
                    <div class="input-field-container">
                        <input type="text" id="name" name="name" placeholder="name" value="<%= (old && old.name) ? old.name : '' %>" required>
                    </div>
                </div>

                <div class="input-group">
                    <label for="nim">NIM</label>
                    <div class="input-field-container">
                        <input type="text" id="nim" name="NIM" placeholder="NIM" value="<%= (old && old.NIM) ? old.NIM : '' %>" required>
                    </div>
                </div>

                <div class="input-group">
                    <label for="password">Password</label>
                    <div class="input-field-container">
                        <input type="password" id="password" name="password" placeholder="Password" required>
                    </div>
                </div>

                <div class="input-group">
                    <label for="password">Ponfirmasi Password</label>
                    <div class="input-field-container">
                        <input type="password" id="password" name="passwordConfirm" placeholder="Konfirmasi password" required>
                    </div>
                </div>

                <button type="submit" class="register-btn">Register</button>
                
            </form>
        </div>
    </div>

</body>
</html>
